# Use what's under your cursor for find and replace
# Hover cursor over banana
# Start the find replace command
# :%s/strawberry/<c-r><c-w>
# At the end hit Ctrl-r <c-r> and Ctrl-w <c-w>
basket <- c("orange", "banana", "orange", "strawberry")

